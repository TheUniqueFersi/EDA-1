#include <stdio.h>
#include <string.h>
#include "video.h"
#include "menu.h"

int main()
{
    // struct Video miVideo;
    // strcpy(miVideo.nombre,"lo mas curioso del mundo");
    // strcpy(miVideo.calidad,"HD");
    // miVideo.n_rep = 56372;
    // miVideo.duracion = 15.45;
    // miVideo.fecha.dia = 12;
    // miVideo.fecha.mes = 3;
    // miVideo.fecha.anio = 2024;
    // imprime(miVideo);
    // miVideo = leer();
    // imprime(miVideo);
    ejecutaMenu();
    return 0;
}
