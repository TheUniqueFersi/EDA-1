
//Para probar que valor devielve la funcion atoi de verdad en caso de desbordamiento
                // scanf("%d", &entero);
                // printf("%d\n\n", entero);
                // printf("ATOI: %d", atoi(cadena));

//Que pasa cuando se define un desbordamiento de int
                // int a = -2147483649, b=1000000000;
                // printf("%d", a);